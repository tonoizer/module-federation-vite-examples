{
  "name": "vue",
  "description": "Module Federation Vite + Vue3 is now possible",
  "scripts": {
    "dev": "pnpm --filter vue-spa-* dev",
    "build": "pnpm --filter vue-spa-* build",
    "preview": "pnpm --filter vue-spa-* preview"
  }
}
